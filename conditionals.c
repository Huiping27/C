#include <stdio.h>

int x = 50; // initialize x with a sample value, you can change it as needed

int main() {
    if(x>10 && x< 100) {
        printf("x is greater than 10 and less than 100,%d\n", x);
    } else {
        printf("x is less than 10 and greater than 99!%d\n",x);
    }
return 0;
}
